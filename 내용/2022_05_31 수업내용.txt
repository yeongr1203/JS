
-- 577page



-- localStorage / sessionStorage
객체를 문자열로 할 수 있음.

//
console 옆에 Application을 들어가면 Storage안에 뭐가 들어 있는지 보임.

1.로컬스토리지에 저장하는 방법  / 2. 쿠키에 저장하는 방법.(이걸 더 추천)

로컬과 세션의 공통점.
둘의 목적은 값저장 같음! 하지만 스코프가 달라진다.
스코프가 달라지지만 저장할수있는 값은 문자열뿐이다.

// 같은 주솟값 기준으로
localStorage =>브라우저를 꺼도 남아있음.
탭을 꺼도 살아있음.

SessionStorage => 브라우저를 끄면 자체가 다 날아감.
탭만 바뀌어도 사라진다.
새로고침은 살아 있음.



객체를 그대로 객체로 저장되지 않기 때문에 문자열로 변경해야하는데, 그 문자열로 변경하여 표현한 것을 JSON이라고 한다. 그 문자열로 저장하고, 그 문자열을 가지고와서 객체로 변경해서 가지고 옴.
객체 => 문자열로 변경하는것. 그것을 (JSON)이라고 한다.	// 문자열로 표현을 했다라고 생각하면 됨.
문자열로 표현해서 저장함.
이 문자열을 사용하기 편하도록 객체로 변경해야함.

문자열을 가지고 와서 객체로 변경함.


--- location

host정보에 :5500(은 포트번호), hostname = 포트번호 뺀 번호
href = origin + pathname




----javascript

setTimeout 함수는 



